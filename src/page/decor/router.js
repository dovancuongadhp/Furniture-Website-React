import { TypeLayout } from "../../layouts/type";
import Decor from "./Decor";

export const routes = [
    {
        path : '/decor',
        element : Decor,
        layout : TypeLayout.PrimaryLayout,
    }
   
]