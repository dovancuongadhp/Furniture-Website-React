import React from "react";

export default function LockIcon({size,color}) {
  return <span className="material-symbols-outlined icon-style" style={{fontSize:size,color:color,marginRight:'5px'}}>lock</span>;
}
